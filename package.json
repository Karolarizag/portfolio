{
  "scripts": {
    "watch": "tailwindcss -i styles/tailwind.css -o style.css --watch",
    "minify": "tailwindcss -i styles/tailwind.css -o style.css --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.5"
  },
  "dependencies": {
    "particles.js": "^2.0.0"
  }
}
